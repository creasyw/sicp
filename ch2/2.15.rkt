;; Her answer is right. Basically, every operation would increase the
;; "uncertainty". As a result, if we can simplify the representation
;; of the formula first and then solve the intervals, we're able to
;; have the tightest percentage.
